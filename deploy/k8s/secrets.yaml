# =============================================================================
# Secrets
# =============================================================================
# NOTE: In production, use external secret management (Vault, AWS Secrets Manager, etc.)
# This file is for reference only - DO NOT commit real secrets

apiVersion: v1
kind: Secret
metadata:
  name: oil-trading-secrets
  namespace: oil-trading
type: Opaque
stringData:
  # Database credentials
  DATABASE_USER: "trading"
  DATABASE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  
  # Redis password (if enabled)
  REDIS_PASSWORD: ""
  
  # JWT secret for authentication
  JWT_SECRET: "GENERATE_A_SECURE_RANDOM_SECRET"
  
  # API Keys (optional - leave empty if not used)
  BLOOMBERG_API_KEY: ""
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  
  # Alert channel credentials (optional)
  SMTP_USER: ""
  SMTP_PASSWORD: ""
  TELEGRAM_BOT_TOKEN: ""
  SLACK_WEBHOOK_URL: ""
  TWILIO_ACCOUNT_SID: ""
  TWILIO_AUTH_TOKEN: ""

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: oil-trading
type: Opaque
stringData:
  POSTGRES_USER: "trading"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  POSTGRES_DB: "trading"
